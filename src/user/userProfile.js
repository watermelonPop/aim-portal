import { useEffect, useState, useRef } from 'react';

function UserProfile({ userInfo, settingsTabOpen }) {
        return (
        
                <h2
                        className='dashboardTitle'
                >
                        USER PROFILE
                </h2>
        );
}

export default UserProfile;